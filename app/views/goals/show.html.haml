%h4= "Goal ##{@goal.id}: #{@goal.title}"

%b= link_to @goal.issue_path, @goal.issue_path
%br
Created By
%b= link_to @goal.created_by, User.github_path(@goal.created_by)

%hr

%h4= "#{@goal.reviews.count} Reviews"
= render @goal.reviews

%hr

= markdown @goal.description

%hr

= link_to 'Edit', edit_goal_path(@goal)
\|
= link_to 'Back', goals_path
